package com.fpt.producerworkbench.common;

public enum MilestoneChatType {
    INTERNAL,  // Group chat nội bộ (chỉ có owner và team members, không có client)
    CLIENT     // Group chat với khách hàng (có client trong danh sách)
}

